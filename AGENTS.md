Study Intake V2 CS408 portable source closure

CurrentQuestionContext v1 is the answer-safe current-question boundary. The
managed current-question entry publishes one private
current-question-evidence-bundle-v3 and keeps formal_write_count=0. Failure
evidence uses current_question_failure_standing_policy_v1 and remains
awaiting_daily_curation.

Luna may consume one complete background candidate, but Luna never writes
formal nodes, relations, mastery, or the capture ledger. Sol owns explicit-date
curation and field-level verification. Formal curation is serial, batch size=1.

Ordinary and morning admission remain unchanged in Phase 1. Retired queue and
apply entrypoints reject new submissions and do not create queue state.
