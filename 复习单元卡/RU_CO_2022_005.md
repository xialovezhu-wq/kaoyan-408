---
schema: review_unit_card_v2
review_unit_id: RU_CO_2022_005
subject: 计算机组成原理
module: CO03 存储器层次结构
schedule_state: recalibration_required_after_rebuild
---

# RU_CO_2022_005 · 分页地址转换中，按页大小拆出页内偏移和虚页号后，必须先统一虚页号进制再查页表，并按存在位决定是否缺页或拼接页框号

- 覆盖正式节点：CO_2022_005
- 旧复习单元：RU_CO_2022_005
- 当前状态：recalibration_required_after_rebuild

## 闭卷提取

闭卷且不看原题、选项或答案：面对“由虚拟地址拆分出的虚页号和页内偏移查页表，判断是否缺页或得到物理地址”，先说出第一动作，再写出完整判断/计算链，并解释每一步为什么必要。

先独立作答并记录置信度、反应时与是否使用提示，再展开下方自查口径。

> [!answer]- 派生自查口径（首答后展开）
> 核心机制：分页地址转换中，按页大小拆出页内偏移和虚页号后，必须先统一虚页号进制再查页表，并按存在位决定是否缺页或拼接页框号
>
> 达标标准：
> - CO_2022_005 知识点：CO03-26 地址转换
> - CO_2022_005 安全摘要：题目给出主存地址位数、分页虚拟存储管理方式、虚拟地址空间大小、页大小、编址单位、待访问虚拟地址和页表片段，要求判断虚实地址转换结果。
> - CO_2022_005 关键参数核验：虚拟地址空间为 4GB；主存地址为 24 位；页大小为 4KB；按字节编址；待访问虚拟地址以十六进制给出；页表片段包含虚页号、页框号和存在位，其中虚页号列未标 H，复做时需先统一进制口径。
> - CO_2022_005 错误入口核验：2026-07-05 入库：能根据页大小拆出页内偏移，但把虚拟地址高位的十六进制虚页号直接当成页表中的十进制虚页号行，忽略页表虚页号列未标 H 时默认按十进制理解，导致查错页表项并误判为缺页。
> - CO_2022_005 第一动作核验：先由页大小确定页内偏移位数；再按十六进制地址切出页内偏移和虚页号；查页表前确认虚页号列是否带 H，若不带 H 就先把虚页号从十六进制转为十进制；最后看存在位，存在位有效时用页框号拼回原页内偏移。
> - CO_2022_005 机制边界：分页地址转换中，按页大小拆出页内偏移和虚页号后，必须先统一虚页号进制再查页表，并按存在位决定是否缺页或拼接页框号
> - 仅在首次闭卷作答后显示以上核验口径；第一动作、关键中间状态或单位、边界解释均完整，且不是只报结论；多源单元需随机抽取至少两个变式方向。
> - 缺任一关键步骤时，先给最小纠偏；3-5 分钟后无提示二次提取。反馈不展示原题答案、选项或既往错选。

## 迁移生成规则

更换表面情境、参数或问法，但保持同一底层机制；先说明识别信号与第一动作，再完成边界判断。迁移通过与保持通过分开记录。

## 调度与证据边界

- 迁移规则：保留该旧单元的真实复习与评分历史，按最后真实复习日和用户分重算；不沿用 2026-07-04 清账顺延产生的日期。 原始建议：不改写用户既有评分；下一次实际复盘后再更新。
- 当日到期集合必须全覆盖；批次只组织顺序，不删除项目。
- 同日反馈后重答成功不冒充跨日保持成功。
- 未经用户明确给出 0–5 分，不更新正式掌握度。
- 原题复做轨另行执行 7 天外硬筛选；本卡不保存完整题干、答案或解析。
