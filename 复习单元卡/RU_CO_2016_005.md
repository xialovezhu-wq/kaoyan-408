---
schema: review_unit_card_v2
review_unit_id: RU_CO_2016_005
subject: 计算机组成原理
module: CO03 存储器层次结构
schedule_state: recalibration_required_after_rebuild
---

# RU_CO_2016_005 · 顺序访问数组时由 Cache 块容量、读写访问次数和初始为空状态计算数组访问缺失率

- 覆盖正式节点：CO_2016_005
- 旧复习单元：RU_CO_2016_005
- 当前状态：recalibration_required_after_rebuild

## 闭卷提取

闭卷且不看原题、选项或答案：面对“顺序数组访问过程中的 Cache 缺失率判断”，先说出第一动作，再写出完整判断/计算链，并解释每一步为什么必要。

先独立作答并记录置信度、反应时与是否使用提示，再展开下方自查口径。

> [!answer]- 派生自查口径（首答后展开）
> 核心机制：顺序访问数组时由 Cache 块容量、读写访问次数和初始为空状态计算数组访问缺失率
>
> 达标标准：
> - CO_2016_005 知识点：CO03-20 Cache 命中率
> - CO_2016_005 安全摘要：给定一段顺序修改数组元素的 C 语言循环、int 宽度、数据 Cache 容量、块大小、映射方式和初始为空条件，求程序执行过程中访问数组的 Cache 缺失率。
> - CO_2016_005 关键参数核验：int 型数据占 4B；Cache 块大小 16B；数据 Cache 数据区大小 1KB；直接映射；循环顺序访问 1000 个数组元素；循环体对同一数组元素先读后写；执行前 Cache 为空；变量 k 不作为数组访问统计对象。
> - CO_2016_005 错误入口核验：2026-07-05 入库：已能根据块大小判断一个 Cache 块可容纳 4 个 int，并知道缺失时会整块调入，但把每个数组元素只按一次访问统计，漏掉赋值语句中对同一数组元素的读访问和写访问两次计数，导致把每块内的 1 次缺失除以 4 次元素访问而不是除以 8 次数组访问。
> - CO_2016_005 第一动作核验：先数数组访问次数：对赋值语句中的同一数组元素区分读和写；再按块大小算每块容纳几个元素；最后用每块首次读缺失次数除以该块内读写总访问次数。
> - CO_2016_005 机制边界：顺序访问数组时由 Cache 块容量、读写访问次数和初始为空状态计算数组访问缺失率
> - 仅在首次闭卷作答后显示以上核验口径；第一动作、关键中间状态或单位、边界解释均完整，且不是只报结论；多源单元需随机抽取至少两个变式方向。
> - 缺任一关键步骤时，先给最小纠偏；3-5 分钟后无提示二次提取。反馈不展示原题答案、选项或既往错选。

## 迁移生成规则

更换表面情境、参数或问法，但保持同一底层机制；先说明识别信号与第一动作，再完成边界判断。迁移通过与保持通过分开记录。

## 调度与证据边界

- 迁移规则：保留该旧单元的真实复习与评分历史，按最后真实复习日和用户分重算；不沿用 2026-07-04 清账顺延产生的日期。 原始建议：不改写用户既有评分；下一次实际复盘后再更新。
- 当日到期集合必须全覆盖；批次只组织顺序，不删除项目。
- 同日反馈后重答成功不冒充跨日保持成功。
- 未经用户明确给出 0–5 分，不更新正式掌握度。
- 原题复做轨另行执行 7 天外硬筛选；本卡不保存完整题干、答案或解析。
