# OS_2023_004

## 安全节点卡片

- 正式节点 ID：OS_2023_004
- 来源 ID：EXAM408-2023-Q31
- 年份：2023
- 科目：操作系统
- 主模块：OS04 文件管理
- 主知识点：OS04-06 文件打开与关闭
- 副知识点：OS04-04 inode；OS04-22 硬链接
- 命中知识点：OS04-06 文件打开与关闭；OS04-04 inode；OS04-22 硬链接
- 详情入口：规范真题语料 EXAM408-2023-Q31；2023 年统考全卷第 31 题；章节别名 章节定位 55 / 本地章节卡片 OS04-4_1-MCQ55；详情库：obsidian://open?vault=kaoyan-408-details&file=cards%2FOS_2023_004
- 错因证据状态：explicit_cause
- 错因证据复核日期：2026-07-12

## 复做安全信息

- 安全题目摘要：在单个进程持有某文件打开引用的场景中，判断该进程关闭文件时必须处理的对象，并区分目录项、内存活动 inode、磁盘 inode 与文件数据空间。
- 关键题设参数：单个文件；仅一个进程持有打开引用；关闭操作；内存活动 inode/系统打开文件表；磁盘 inode 硬链接计数；目录项与磁盘数据块。
- 问法类型：判断最后一个打开引用关闭后必须更新或释放的对象
- 实际考查机制：先画两条独立关系链：目录项到磁盘 inode（硬链接计数）与 fd 到系统打开文件表/内存活动 inode（打开引用计数）；再只沿题目操作所在链更新计数，最后判断回收对象位于内存还是磁盘。
- 用户错误入口：2026-07-12 入库：把 open/close 对内存打开引用计数的影响误套到磁盘 inode 的硬链接计数，因而认为 close 会使硬链接计数减一；同时把内存索引结点占用空间误读为文件磁盘数据空间。 2026-07-12 晚间复盘再次错误：把系统打开对象的打开引用误称为硬链接，并认为进程退出等同于删除文件描述符、open 或 close 会改变磁盘 inode 的硬链接计数；同时混淆单个进程表项随描述符关闭而释放、系统级表项在最后打开引用归零后释放，以及磁盘 inode 和数据块需硬链接计数与打开引用都归零后才回收的条件。 2026-07-16 晨间复盘再次错误：已识别硬链接关系与文件打开关系，但未展开目录项到磁盘 inode、文件描述符到打开文件表两条对象链，也未区分硬链接计数与打开引用计数。 2026-07-20 晨间复盘再次错误：正确判断 close 不改变硬链接计数，但把打开引用计数归到磁盘 inode，并将打开引用归零误作回收数据块的充分条件，未同时核验硬链接计数；随后完成无提示独立纠正。 2026-07-18 复做再次错误：首次判断unlink删除最后目录项后已有FD不能继续访问，混淆路径名访问与已有打开引用 2026-07-24 复做再次错误：把内存活动 inode 说成磁盘活动 inode 2026-07-25 复做再次错误：认为内存活动 inode 保存在磁盘上
- 复做第一动作：先画两条独立关系链：目录项到磁盘 inode（硬链接计数）与 fd 到系统打开文件表/内存活动 inode（打开引用计数）；再只沿题目操作所在链更新计数，最后判断回收对象位于内存还是磁盘。
- 核心考点：区分内存活动 inode 的打开引用计数与磁盘 inode 的硬链接计数，并判断最后一个打开引用关闭时的资源释放范围
- 模糊概念：打开引用计数 vs 硬链接计数；内存活动 inode vs 磁盘 inode；close vs unlink；释放内存控制结构 vs 回收磁盘数据块
- 错因标签：E01 概念边界混淆
- 关系复核：connected
- 关系复核日期：2026-07-12
- 语义证据等级：A

## 附件登记

- 题图：已接收并保存 1 张，位于 details vault
- 解析图或解析正文：已接收并保存解析图及折叠解析，位于 details vault
- 作答痕迹：用户口述作答与排除过程，未提供手写痕迹
- 可视化详情入口：cards/OS_2023_004.md
- 当前附件来源：本次对话附件已转存至 details vault
- OCR/解析处理：只提取安全题目摘要、关键题设参数、问法类型、错误入口和复做第一动作；不复制完整题干、答案或完整解析
- 复做保护：已隐藏答案、选项字母组合、用户错选和标准解析中的答案结论

## 复做保护

- 复做保护说明：本卡只保留题目骨架、必要题设参数、错误入口和复做动作；不保存完整题干、完整解析、标准答案、错选项、选项字母、图片或手写痕迹。
